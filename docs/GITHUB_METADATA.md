# GitHub metadata

Suggested description: Native Web Components renderer for A3UI surface semantics.

Suggested topics: `web-components`, `typescript`, `accessibility`, `a3ui`, `conformance`.

These are manual GitHub settings; no remote metadata is changed by this repository.